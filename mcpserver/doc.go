// Package mcpserver adapts the engram memory engine to the MCP protocol.
//
// The package is deliberately independent of the engine's implementation
// details: it only composes and calls the engine's public APIs.
package mcpserver
